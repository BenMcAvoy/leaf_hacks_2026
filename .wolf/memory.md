# Memory Log

- 2026-07-04: Installed `genkit` and `@genkit-ai/googleai` via bun. Not yet wired into any route or lib file.
- 2026-07-04: UX review of full app. Merged three duplicated views into shared components (study-pack-list, accessibility-controls, learning-style-selector); fixed profile page silently missing text-size/line-spacing controls that onboarding exposes; made dashboard quick-action cards actually diverge (they all routed to the same /upload form with no distinguishing signal). Flagged but did not change: /packs has no bottom-nav entry point, and pack quiz breaks the tab-based navigation pattern by routing to a separate page. Build/typecheck/lint all clean after changes.
- 2026-07-04: Verified goal "build 100% green and lints 100% green" - `npm run build` and `npm run lint` both pass with exit code 0, no changes needed.
- 2026-07-04: Added a visible light/dark mode toggle. The app already had full light-mode CSS support and a working `next-themes` setup, but the only way to switch themes was an undocumented `d` keyboard shortcut - no UI control existed anywhere. Added `components/theme-toggle.tsx` (sun/moon icon button, same toggle logic as the hotkey) and wired it into `components/nav-shell.tsx` header. Build and lint clean. Verified dev server boots with no runtime errors; could not click-through the actual toggle in the authenticated shell since that requires Firebase auth credentials not available in this env (same limitation as prior Brainiac verification).
- 2026-07-04: Follow-up: switched `app/layout.tsx` `defaultTheme` from "dark" to "light" so light mode is now the app default, and added `components/appearance-controls.tsx` (Light/Dark/System button group using `useTheme()` directly, not persisted to Firestore) into `app/(app)/profile/page.tsx`'s settings card, above Accessibility. Build and lint clean.
- 2026-07-04: Added "Brainiac" mascot feature - contextual SVG/CSS character pop-ins that react to AI assistant state. New: `components/providers/brainiac-provider.tsx` (useBrainiac hook: show(mood, message?, durationMs?), hide()), `components/brainiac-mascot.tsx` (renders in bottom-left corner, z-40, under chat bubble z-50 and toast z-9999). Wired into `components/chat-bubble.tsx` (greeting on open, thinking while sending, happy/error on reply) and `app/(app)/dashboard/page.tsx` (one-time welcome greeting on mount). Mounted in `app/(app)/layout.tsx` alongside ActivePackProvider/ChatBubble (authenticated shell only). All motion is plain CSS keyframes added to `app/globals.css` near existing `home-*`/`toast-*` keyframes, so it automatically respects the existing `html.reduce-motion` global override with no extra detection logic. Caught and fixed a real "setState during render of another component" bug where brainiac.show() was called inside a useState updater callback - see buglog.json. Typecheck/lint clean; verified via live dev server logs (no headless browser tooling available in this env without adding a new dependency, and the app requires Firebase auth I don't have test credentials for, so full click-through wasn't performed).
- 2026-07-04: Added definition-mode quiz + Gemini voice mode. New: app/(app)/pack/[id]/quiz/define/page.tsx (definition quiz with text or voice answer, Gemini grading, XP/streak parity with MCQ mode); app/api/quiz/grade-definition/route.ts; app/api/quiz/speak/route.ts; lib/audio.ts (PCM to WAV wrapper). Extended: lib/types.ts (UserProfile.voiceModeEnabled), lib/genkit.ts (+ttsModel), lib/ai.ts (+gradeDefinitionAnswer, +synthesizeSpeech), app/(app)/profile/page.tsx (+Voice mode Switch toggle), app/(app)/pack/[id]/page.tsx (+Definition mode button in quiz tab). TTS uses gemini-2.5-flash-preview-tts; STT sends MediaRecorder audio/webm as data URL to the grade endpoint so Gemini transcribes+grades in one call. Build and lint clean.
- 2026-07-04: Added mic input and AI-driven navigation to the chat assistant (components/chat-bubble.tsx). Mic button (input row, gated behind existing UserProfile.voiceModeEnabled) reuses the definition-quiz's MediaRecorder audio/webm -> base64 -> FileReader pattern, no new STT route - audio is folded straight into POST /api/chat, which now accepts optional `audio` instead of requiring `message`. lib/ai.ts's chatReply switched from returning a plain string to Genkit structured output (`z` schema, same pattern as generateStudyPack/gradeDefinitionAnswer) returning `{reply, navigateTo, transcript?}`; navigateTo is a closed 6-value enum (CHAT_NAV_TARGETS) so the model can never emit an arbitrary path, resolved to a real URL server-side via resolveChatNavRoute(target, activePackId) in route.ts using the already ownership-checked active pack. Client calls router.push(data.navigateTo) from next/navigation when present. Build and lint clean; dev server boots with no console errors. Could not click-through the mic/nav flow live since Firebase auth credentials aren't available in this environment (same limitation as prior voice-mode and Brainiac verifications).
